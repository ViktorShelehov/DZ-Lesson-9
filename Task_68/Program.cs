//Напишите программу вычислинения функции Аккермана с помощью рекурсии.
//Даны два неотрицательных числа M и N. 
//m = 2, n = 3 -> A(m,n) = 9
Console.Clear();

int m = GetNumberFromUser("Введите первое число М: ", "Ошибка ввода! ");
int n = GetNumberFromUser("Введите второе число N: ", "Ошибка ввода! ");

Console.WriteLine(Ackerman(m,n));

///////////////////////////////////////////////////////////////////////////////
///////////////////////- Метод ввода данных -/////////////////////////////////
/////////////////////////////////////////////////////////////////////////////

int GetNumberFromUser(string message, string ErrorMessage)
{
    while(true)
    {
        Console.Write(message);
        if(int.TryParse(Console.ReadLine(), out int userNumber))
        return userNumber;
        Console.Write(ErrorMessage);
    }
}
////////////////////////////////////////////////////////////////////////////////
////////////////////////- Решение задачи -/////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////
int Ackerman(int m, int n)
		{
			if(m == 0)
			{
				return n + 1;
			}
			if(m > 0 && n == 0)
			{
				return Ackerman(m - 1, 1);
			}
			return Ackerman(m - 1, Ackerman(m, n - 1));
		}
////////////////////////////////////////////////////////////////////////////////
/////////////////////////////- Конец -/////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////